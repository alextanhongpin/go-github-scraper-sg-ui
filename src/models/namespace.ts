export const user = {
  namespace: 'user'
}

export const repo = {
  namespace: 'repo'
}

export const recommendation = {
  namespace: 'recommendation'
}

export const language = {
  namespace: 'language'
}
